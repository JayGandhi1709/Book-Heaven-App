class AddCarouselScreen extends StatelessWidget {
  const AddCarouselScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scafold(
      body:Text("Add Carousel Screen");
    );
  }
}
